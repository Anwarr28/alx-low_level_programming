#include "main.h"

/**
  * main - Entry point.
  * @argc: the number of arguments.
  * @argv: the arguments names.
  *
  * Description: program prints its name.
  * Return: 0 (Success).
  */
int main(int argc __attribute__((unused)), char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
