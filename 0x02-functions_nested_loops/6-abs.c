#include <stdio.h>
#include "main.h"

/**
  * _abs - compute the apsolute value of an integer.
  * @n: the number to be computed.
  *
  * Return: the new number of type int.
  */

int _abs(int n)
{
	n = n * -1;
	return (n);
}
